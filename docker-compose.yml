version: '3.8'

services:
  dashboard:
    build: .
    container_name: eric_dashboard_dev
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql://postgres.fjmcadkjxlnibmgmcxwn:RobotSandwichThrone@aws-1-us-west-2.pooler.supabase.com:6543/postgres
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    restart: unless-stopped
